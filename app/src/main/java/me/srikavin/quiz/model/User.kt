package me.srikavin.quiz.model

/**
 * A data class that represents a logged-in user.
 */
open class User(
        /**
         * The username the user has signed up under
         */
        val username: String
)
